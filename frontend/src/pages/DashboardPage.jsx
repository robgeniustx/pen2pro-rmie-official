import { getCurrentUser } from "../lib/auth";
export default function DashboardPage({ navigateTo }) { const u=getCurrentUser(); return <main className="auth-page__main" style={{minHeight:'85vh'}}><div className="auth-card"><h1 className="auth-card__title">Welcome, {u?.name || u?.email}</h1><p className="auth-card__sub">Current Tier: <strong>{u?.tier || 'free'}</strong></p><button className="auth-card__submit" onClick={()=>navigateTo('/starter?tier=free')}>Start Free Blueprint</button><button className="auth-card__submit" onClick={()=>navigateTo('/pricing')}>Upgrade to Pro</button><button className="auth-card__submit" onClick={()=>navigateTo('/pricing')}>Upgrade to Elite</button><p>Saved Blueprints (coming soon)</p><p>Recent Business Ideas (coming soon)</p><button className="auth-card__submit" onClick={()=>navigateTo('/account')}>Account Settings</button></div></main>; }
