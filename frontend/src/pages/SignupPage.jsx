import { useState } from "react";
import { signupUser } from "../lib/auth";
export default function SignupPage({ navigateTo }) { const q=new URLSearchParams(window.location.search); const [name,setName]=useState(""); const [email,setEmail]=useState(""); const [password,setPassword]=useState(""); const [confirm,setConfirm]=useState(""); const [businessIdea,setBusinessIdea]=useState(""); const [tier]=useState(q.get('tier')||'free'); const [error,setError]=useState(""); const submit=async()=>{ if(password!==confirm){setError('Passwords do not match.');return;} await signupUser({name,email,password,businessIdea,tier}); navigateTo('/dashboard');}; return <main className="auth-page__main" style={{minHeight:'85vh'}}><div className="auth-card"><h1 className="auth-card__title">Create PEN2PRO Account</h1>{error&&<div className="auth-card__error">{error}</div>}<label className="auth-field"><span>Full name</span><input value={name} onChange={e=>setName(e.target.value)} /></label><label className="auth-field"><span>Email</span><input type="email" value={email} onChange={e=>setEmail(e.target.value)} /></label><label className="auth-field"><span>Password</span><input type="password" value={password} onChange={e=>setPassword(e.target.value)} /></label><label className="auth-field"><span>Confirm password</span><input type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} /></label><label className="auth-field"><span>Business idea (optional)</span><input value={businessIdea} onChange={e=>setBusinessIdea(e.target.value)} /></label><p className="auth-card__sub">Tier: {tier}</p><button className="auth-card__submit" onClick={submit}>Create account</button></div></main>; }
