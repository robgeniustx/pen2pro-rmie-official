import { useState } from "react";
import { loginUser } from "../lib/auth";
export default function LoginPage({ navigateTo }) { const [email,setEmail]=useState(""); const [password,setPassword]=useState(""); const [error,setError]=useState(""); const submit=async()=>{ try{ await loginUser(email,password); navigateTo('/dashboard'); }catch(e){ setError(e.message);} }; return <main className="auth-page__main" style={{minHeight:'85vh'}}><div className="auth-card"><h1 className="auth-card__title">PEN2PRO Sign In</h1><p className="auth-card__sub">Build your business roadmap. Save your blueprint. Upgrade when ready.</p>{error&&<div className="auth-card__error">{error}</div>}<label className="auth-field"><span>Email</span><input type="email" value={email} onChange={(e)=>setEmail(e.target.value)} /></label><label className="auth-field"><span>Password</span><input type="password" value={password} onChange={(e)=>setPassword(e.target.value)} /></label><button className="auth-card__submit" onClick={submit}>Sign In</button><p><button className="auth-card__toggle-btn" onClick={()=>navigateTo('/signup')}>Create Account</button></p><p><button className="auth-card__toggle-btn">Forgot Password?</button></p></div></main>; }
