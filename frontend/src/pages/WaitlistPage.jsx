import { useState } from "react";
export default function WaitlistPage(){ const [ok,setOk]=useState(false); const [form,setForm]=useState({name:'',email:'',stage:'idea',building:''}); const submit=(e)=>{e.preventDefault(); const key='pen2pro_waitlist'; const rows=JSON.parse(localStorage.getItem(key)||'[]'); rows.push({...form,createdAt:new Date().toISOString()}); localStorage.setItem(key,JSON.stringify(rows)); setOk(true);}; return <main className="auth-page__main" style={{minHeight:'85vh'}}><form className="auth-card" onSubmit={submit}><h1 className="auth-card__title">Join Waitlist</h1><label className="auth-field"><span>Name</span><input value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label className="auth-field"><span>Email</span><input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label className="auth-field"><span>Business stage</span><select value={form.stage} onChange={e=>setForm({...form,stage:e.target.value})}><option>idea</option><option>mvp</option><option>launched</option><option>scaling</option></select></label><label className="auth-field"><span>What are you building</span><textarea value={form.building} onChange={e=>setForm({...form,building:e.target.value})}/></label><button className="auth-card__submit">Join Waitlist</button>{ok&&<p>Success! You are on the waitlist.</p>}</form></main>; }
