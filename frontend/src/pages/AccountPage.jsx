import { getCurrentUser, logoutUser } from "../lib/auth";
export default function AccountPage({ navigateTo }) { const u=getCurrentUser(); return <main className="auth-page__main" style={{minHeight:'85vh'}}><div className="auth-card"><h1 className="auth-card__title">Account</h1><p>Name: {u?.name}</p><p>Email: {u?.email}</p><p>Current tier: {u?.tier || 'free'}</p><p>Subscription status: Active placeholder</p><button className="auth-card__submit" onClick={()=>{logoutUser();navigateTo('/login')}}>Logout</button><button className="auth-card__submit">Manage billing (placeholder)</button><button className="auth-card__submit" onClick={()=>navigateTo('/dashboard')}>Return to dashboard</button></div></main>; }
