import { createFounderCheckout } from "../services/api";
async function handlePaidTier(tier, navigateTo){ try{ const data = await createFounderCheckout(tier); if(data?.checkoutUrl||data?.url){ window.location.href=data.checkoutUrl||data.url; return; } }catch{} navigateTo(`/signup?tier=${tier}`); }
export default function PricingPage({ navigateTo }) { return <main className="auth-page__main" style={{minHeight:'85vh'}}><div className="auth-card"><h1 className="auth-card__title">Pricing</h1><button className="auth-card__submit" onClick={()=>navigateTo('/starter?tier=free')}>Free</button><button className="auth-card__submit" onClick={()=>handlePaidTier('pro',navigateTo)}>Pro</button><button className="auth-card__submit" onClick={()=>handlePaidTier('elite',navigateTo)}>Elite</button><button className="auth-card__submit" onClick={()=>handlePaidTier('founder',navigateTo)}>Founder</button></div></main>; }
